# LeadFlow for Borg

Lead automation frontend built with Vite + React + shadcn/ui.

This app supports two modes:
- Real mode: calls Power Automate HTTP endpoints from `.env`
- Local demo mode: no external services required, uses browser local storage
